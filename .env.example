# ==================== Imagora 环境配置模板 ====================
# 用法：复制本文件为 .env 后填写（.env 已被 git 忽略，不会泄露）
# 也可不建文件，直接用系统环境变量（环境变量优先于 .env）
#
# 配置分层（详见 ARCHITECTURE.md 配置加载层）：
#   公开配置（中转站/模型/尺寸/质量/ratios）→ config.json（git 跟踪，多 profile）
#   密钥 + 本机覆盖 → 本文件（git 忽略）

# ---------------- API Key（必填其一）----------------
# 推荐：按 profile 命名，切换中转站（ACTIVE_PROFILE）时 key 自动跟随
#   当前默认 profile 是 wanwu → 配 API_KEY_WANWU
API_KEY_WANWU=sk-在这里填你的Key
# 通用回退（任意 profile 都可用）：
#API_KEY=sk-你的Key
# 兼容旧写法（老用户零改动，仍会回退使用）：
#AIWANWU_API_KEY=sk-你的Key

# ---------------- 切换中转站（可选）----------------
# 不填 = 使用 config.json 的 "default_profile"（团队公共默认）
# 本机临时切换示例（不改仓库里的公共默认）：
#ACTIVE_PROFILE=other
# 配套给 other 配 key：
#API_KEY_OTHER=sk-other站的Key
# 或者在 config.json 注册新的
